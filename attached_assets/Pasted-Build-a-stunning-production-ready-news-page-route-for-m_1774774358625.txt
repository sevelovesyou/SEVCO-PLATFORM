Build a stunning, production-ready /news page (route) for my existing website.

Use Next.js 15 App Router + TypeScript + Tailwind CSS + shadcn/ui. Keep the existing project structure and styling system intact. Add any new dependencies you need.

The entire page must showcase **EVERY cool feature of the official xAI Grok API and SDK** in a beautiful, real-world news aggregator:

### Core xAI Features to use (MANDATORY - use all of them):
- Latest Grok reasoning model (grok-4.20 or newest available)
- Full tool calling: `web_search` + `x_search` (X/Twitter search) for real-time news and social buzz
- Structured outputs / JSON mode for clean, consistent data
- Streaming responses (where it improves UX)
- Grok Imagine (text-to-image) to auto-generate unique, high-quality header and thumbnail images for every story
- Vision capabilities if relevant (analyze any news images)
- Agentic behavior: Grok should intelligently decide when to call tools, summarize, reason, and generate visuals

### Page Features & Layout (Modern dark theme, very premium, Grok/xAI aesthetic):
- Hero section with the top story + massive Grok-generated image
- Responsive masonry/grid of news cards (headline, Grok summary, custom Grok Imagine image, source, timestamp, category tag, "Read with Grok" button)
- Sidebar:
  - "Trending on X" (live X search results with Grok commentary)
  - Quick categories (Tech, World, Business, AI, Science, etc.)
  - "Ask Grok Anything" mini-chat
- Top navigation bar with global search that accepts natural language ("What's the latest on AI regulation?" → Grok uses tools + reasoning)
- Click any card → modal with:
  - Full Grok-generated deep summary
  - Structured key insights (bullets)
  - Related X posts (via x_search)
  - Option to "Generate custom illustration" (Grok Imagine)
  - Streaming "Ask Grok about this story" chat
- Floating "Generate AI Daily Briefing" button that creates a beautiful, structured daily newsletter-style summary using Grok + tools

### Technical Requirements:
- All xAI calls must go through secure Next.js Route Handlers / Server Actions (never expose API key on client)
- Use @ai-sdk/xai (or OpenAI compatibility layer with xAI base URL) – whichever gives the cleanest access to all features (tools, Imagine, streaming, structured outputs)
- Store `XAI_API_KEY` in Replit Secrets / .env
- Excellent loading states, skeletons, error handling, and optimistic updates
- Responsive on mobile + desktop
- Fast – use caching where sensible
- SEO-friendly metadata and Open Graph tags

First, plan the full architecture and UI layout on the canvas (components, routes, API endpoints). Then build everything step-by-step with beautiful, modern code.

This page should feel like the ultimate demonstration of what the xAI SDK can do – make it jaw-dropping.